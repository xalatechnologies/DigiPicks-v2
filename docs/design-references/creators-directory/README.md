# Creators directory (Stitch zip 63)

Reference for the public `/creators` browse page.

| File         | Use                                           |
| ------------ | --------------------------------------------- |
| `screen.png` | Visual target                                 |
| `code.html`  | Layout, copy, sections                        |
| `DESIGN.md`  | Editorial rules (map to existing tokens only) |

Implemented in `apps/web/src/pages/Creators.tsx` with DS components under `packages/ds/`.
