# Today's Events (Stitch zips 64 & 65)

| Zip    | Focus                                                                                                                     |
| ------ | ------------------------------------------------------------------------------------------------------------------------- |
| **64** | `/events` directory — hero, live strip, featured marquees, sport-grouped schedule, sidebar pick highlight, creator rail   |
| **65** | Event detail patterns — large matchup hero, coverage stats (used as reference for marquee cards and future `/events/:id`) |

Implemented in `apps/web/src/pages/Events.tsx` with DS components under `packages/ds/src/components/domain/`.
